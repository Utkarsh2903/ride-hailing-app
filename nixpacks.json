{
  "phases": {
    "setup": {
      "nixPkgs": ["...", "postgresql", "geos", "proj"],
      "aptPkgs": []
    },
    "install": {
      "cmds": ["bundle install"]
    }
  },
  "start": {
    "cmd": "bundle exec puma -C config/puma.rb"
  }
}

